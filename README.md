
  # Mockup Permisos

  This is a code bundle for Mockup Permisos. The original project is available at https://www.figma.com/design/Q3gYcVQDWhHKN5W5lbbjCc/Mockup-Permisos.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  